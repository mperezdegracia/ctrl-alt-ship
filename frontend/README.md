# Frontend

Static dashboard for a single operation. The first feature will be
`operation`: current status, mandate, quotes, commitment timeline,
escalations, and replay from checkpoints. All UI copy is in English.

The build is served by the backend runtime to keep a single Render deployment.
